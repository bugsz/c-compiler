# C Compiler Frontend


## Compile
* Preresquisites
    * GNU Autotools & Flex/Bison installed
    * A C/C++ compiler with >=`c99` & >=`c++17` support

* Build steps
```bash
sh run-script.sh
./configure
make
```